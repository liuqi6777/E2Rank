# BGE-M3 dense embedding protocol (CLS pooling, no role prefix).
model_name_or_path: BAAI/bge-m3
pooling_method: cls
padding_side: right
append_token: none
query_prompt_template: "{query}"
document_prompt_template: "{document}"
embedding_max_length: 8192

# XLM-R/BERT-style attention projections. Override this list if a custom
# checkpoint exposes different module names.
lora_target_modules: [query, key, value]
deepspeed: ./scripts/zero3.json
