_base_: g2.yaml
# Tokenization v2 and FP32 pooling/scoring are enforced by the shared source.
# Keep the former G2 optimizer/DeepSpeed/checkpoint recipe.
index_cache_dir: .cache/dataset_index/g2-cl-r2
dev_samples_per_source: 0
load_best_model_at_end: false
