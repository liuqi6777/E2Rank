# Mixture weight sweep (M4). See default_mixture.yaml for the argument.
reward_terms:
  - type: ndcg_in_batch
    weight: 1.0
    k: 16
    include_negatives: false
  - type: infonce
    weight: 2.0
    temperature: 0.03
    in_batch_negatives: true
reward_combine: normalized_sum
