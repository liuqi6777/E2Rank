# Differentiable-reward control, restricted to the sample's own slate so it
# matches the candidate set used by direct-backprop InfoNCE.
reward_type: infonce
contrastive_use_in_batch_negatives: false
contrastive_temperature: 0.03
