# New-protocol G2 CL settings; historical experiments.yaml stays independent.
output_dir: checkpoints/iclr2027-g2-cl-r2
G2:
  data: data
  bge_m3_data: /mnt/tidal-alsh-share2/dataset/liuqi18/datasets/bge-m3-data
  model: Qwen/Qwen3-0.6B
  learning_rate: 0.000005
  steps: 1200
  checkpoint_every: 200
  batch_size: 128
  micro_batch_size: 16
